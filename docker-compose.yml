services:
  directus:
    image: directus/directus:latest
    ports:
      - "8055:8055"
    volumes:
      - ./directus/database:/directus/database
      - ./directus/uploads:/directus/uploads
      - ./directus/extensions:/directus/extensions
    user: "${DIRECTUS_UID:-1000}:${DIRECTUS_GID:-1000}" # Linux permissions (override in .env for Proxmox)
    environment:
      KEY: "${DIRECTUS_KEY:-felix-lms-key}"
      SECRET: "${DIRECTUS_SECRET:-felix-lms-secret-must-be-32-chars-long}"
      DB_CLIENT: "sqlite3"
      DB_FILENAME: "/directus/database/data.db"
      ADMIN_EMAIL: "${DIRECTUS_ADMIN_EMAIL:-admin@example.com}"
      ADMIN_PASSWORD: "${DIRECTUS_ADMIN_PASSWORD:-password}"
      CORS_ENABLED: "true"
      CORS_ORIGIN: "true"
      CORS_CREDENTIALS: "true"
      CORS_METHODS: "GET,POST,PUT,PATCH,DELETE,OPTIONS"
      CORS_ALLOWED_HEADERS: "Content-Type,Authorization"
      PUBLIC_URL: "${DIRECTUS_PUBLIC_URL:-http://localhost:8055}"
      DB_SQLITE_JOURNAL_MODE: "DELETE"
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./directus/database:/directus/database
      - ./backend/uploads:/app/uploads
    environment:
      PORT: "${BACKEND_PORT:-5000}"
      NODE_ENV: "${NODE_ENV:-production}"
      DB_PATH: "${DB_PATH:-/directus/database/data.db}"
    depends_on:
      - directus
    restart: unless-stopped

  frontend:
    build:
      context: ./react-frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: "${VITE_API_URL:-/api}"
        VITE_DIRECTUS_URL: "${VITE_DIRECTUS_URL:-/directus}"
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend
    restart: unless-stopped
